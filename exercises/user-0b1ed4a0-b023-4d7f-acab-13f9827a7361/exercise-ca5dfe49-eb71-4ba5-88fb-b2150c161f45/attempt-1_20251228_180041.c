#include <stdio.h>

int main() {
    int a, b;
    int sum;

    // Input two integers
    scanf("%d %d", &a, &b);

    // Calculate sum
    sum = a + b;

    // Output result
    printf("%d", sum);

    return 0;
}